#include <unistd.h>
#include "main.h"

/**
 * _isalpha - cheks for alphabetic characters
 * @c: The character to check
 *
 * Return: 1 if c ia letter, otherwise 0
 */
int _isalpha(int c)
{
	((c >= 0 && c <= 'Z') || (c >= 'A' && c <))
	{
		return (1);
	}
	return (0);
}
